name: Scala CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 8
      uses: actions/setup-java@v2
      with:
        java-version: '8'
        distribution: 'adopt'
    - name: Set up MySQL
    - run: mysql -e "CREATE DATABASE films;"
    - run: mysql -e "USE films; CREATE TABLE films (id int, name varchar(48), year int, country varchar(48), director varchar(48));"
    - run: mysql -e "USE films; INSERT INTO films (id, name, year, country, director) VALUES (8, 'Tenet', 2020, 'UK', 'Christopher Nolan');"
    - run: mysql -e "USE films; INSERT INTO films (id, name, year, country, director) VALUES (9, 'Batman Begins', 2005, 'USA', 'Christopher Nolan');"
    - name: Set up PostgreSQL
    - run: sudo apt update && sudo apt-get install postgresql
    - run: psql -c "CREATE DATABASE films;" -U postgres
    - run: psql -c "CREATE TABLE films (id int, name varchar(48), year int, country varchar(48), director varchar(48));" films -U postgres
    - run: psql -c "INSERT INTO films (id, name, year, country, director) VALUES (8, 'Tenet', 2020, 'UK', 'Christopher Nolan');" films -U postgres
    - run: psql -c "INSERT INTO films (id, name, year, country, director) VALUES (9, 'Batman Begins', 2005, 'USA', 'Christopher Nolan');" films -U postgres
    - name: Run tests
      run: sbt test
